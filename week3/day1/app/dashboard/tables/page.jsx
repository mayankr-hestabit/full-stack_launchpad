import Card from "@/components/ui/Card";

export default function TablesPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-slate-800">Tables</h1>
      <p className="mt-1 text-sm text-slate-500">/dashboard/tables</p>
      <Card className="mt-6">
        <p className="text-sm text-slate-500">
          Placeholder for now — the real data table (mocked users list) is a
          Day 5 deliverable.
        </p>
      </Card>
    </div>
  );
}
