import Card from "@/components/ui/Card";

export default function ChartsPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold text-slate-800">Charts</h1>
      <p className="mt-1 text-sm text-slate-500">/dashboard/charts</p>
      <Card className="mt-6">
        <p className="text-sm text-slate-500">
          Not part of Week 3's required deliverables — placeholder route so the
          sidebar link works. Chart implementation can be added later if needed.
        </p>
      </Card>
    </div>
  );
}
